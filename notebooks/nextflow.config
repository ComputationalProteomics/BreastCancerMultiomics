// nextflow.config
process {
    cpus = 2
    memory = '6 GB'

    // Environment variables for all processes
    env {
        // Export Nextflow CPU allocation to R
        NXF_TASK_CPUS = '$task.cpus'
    }
}
